强模型：能从分析维度的结构中推断出质量期望 → 质量要求是冗余
弱模型：需要显式的质量标准锚点才能维持输出底线 → 质量要求是必需

→ 通用提示词面向不确定能力的模型，必须保留质量标准作为显式约束



分析维度       ← 写什么、怎么分析（内容层）
质量标准       ← 输出要达到什么水平（标准层）← 新增
格式规范       ← 用什么格式、什么图（形式层）
输出前自查     ← 验证是否达标（验收层）